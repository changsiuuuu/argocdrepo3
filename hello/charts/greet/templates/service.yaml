# hello/charts/greet/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: greet-svc
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
  selector:
    app: greet
    